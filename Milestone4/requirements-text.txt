# Test dependencies for Module 4
# Install with: pip install -r milestone_4/requirements-test.txt

# Core test framework
pytest==8.3.2
pytest-cov==5.0.0          # coverage reporting
pytest-asyncio==0.23.8     # async test support

# FastAPI test client (already in milestone_2/requirements.txt but listed here for clarity)
fastapi==0.115.0
uvicorn==0.32.0
pydantic==2.9.2
httpx==0.27.2              # required by TestClient in newer FastAPI versions